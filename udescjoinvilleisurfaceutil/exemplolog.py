

# Exemplo de uso
#if __name__ == "__main__":
    # Cria uma instância do logger
    #logger = Log(log_file=Log.FILE)

    # Registra uma mensagem informativa
    #logger.log_info("Aplicação iniciada com sucesso.")

    # Simula um erro e registra com stack trace
    #try:
        #resultado = 10 / 0  # Gera uma exceção ZeroDivisionError
    #except Exception as e:
        #logger.log_error_with_stack(e)

    # Registra um aviso
    #logger.log_warning("Cuidado: algo pode estar errado.")        